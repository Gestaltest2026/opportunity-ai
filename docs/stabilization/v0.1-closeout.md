# v0.1 Stabilization Closeout

## Status

CLOSED. Deterministic verification passed in a clean GitHub Actions pull-request environment, and the closeout PR was merged to `main`.

Feature Mode may resume.

## Completed architecture work

- Zod is the source of truth for canonical domain schemas and runtime parsing.
- Opportunity availability is canonical; derived databank status is not persisted.
- Databank and Source Registry I/O are isolated behind dedicated I/O modules.
- Structured LLM calls share one boundary for transport, JSON parsing, schema parsing, and error normalization.
- Fixture and ground-truth data are schema-validated.
- LLM verification is separated from deterministic verification.
- Legacy persisted `status` and `source_hash` compatibility state has been removed.
- ADRs record the major architecture decisions.

## Deterministic verification surface

`npm run verify` covers:

1. TypeScript typecheck
2. Semantic hash regression
3. Databank schema regression
4. Fixture schema validation
5. Pure domain regression

Observed result: PASS in GitHub Actions on the final PR head without the temporary CI probe file.

## External / nondeterministic verification

`npm run verify:llm` remains intentionally non-blocking and should run manually, on a schedule, or before release. It is not a required push/PR gate.

## Closeout evidence

- Clean environment dependency installation passed.
- Deterministic verification passed.
- The verification workflow completed successfully on the final PR head.
- PR #1 was merged to `main`.

## Operating rule after v0.1

Feature work may resume, but new features must preserve the stabilization invariants: schema ownership, state ownership, boundary ownership, deterministic regression coverage, and non-blocking LLM verification.
